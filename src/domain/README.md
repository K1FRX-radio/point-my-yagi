# Pure geographic + domain layer

Runtime-independent TypeScript only. Do not import `react-native` or `expo-*` here
so these modules stay unit-testable under Node (Vitest). Populated in Milestone 1.
