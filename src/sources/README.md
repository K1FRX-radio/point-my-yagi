# Target sources

Adapters behind a common typed `TargetSource` interface (Manual, POTA, etc.).
Pure TypeScript with injectable dependencies so mock sources can be used in tests.
Populated starting in Milestone 4.
